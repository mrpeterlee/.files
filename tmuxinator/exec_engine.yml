# /home/peter/.config/tmuxinator/work.yml

name: exec_engine
root: ~/

windows:
  - general:
      layout: even-horizontal
      panes:
        - workspace1:
          - cd $HOME/lab/paper/finclab/finclab/sh/bin
          - exec_engine
